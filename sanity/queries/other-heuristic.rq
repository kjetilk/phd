PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
PREFIX dct: <http://purl.org/dc/terms/>

SELECT ?g ?type (xsd:integer(?heuristic) as ?fresh)
WHERE { GRAPH ?g {
  ?ir <urn:app:status> "OK" ; 
      <urn:app:freshtime:heuristic> ?heuristic .
  ?g  dct:type ?type .
}
} 
